import 'package:flutter/material.dart';

class PreScanScreen extends StatelessWidget {
  const PreScanScreen();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pre Scan Screen'),
      ),
      body: Center(
        child: Text('Pre Scan Screen'),
      ),
    );
  }
}